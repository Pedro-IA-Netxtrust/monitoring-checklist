import type { ReactNode } from 'react';

/** El panel admin ya no vive en el checklist: usar Control de Vehículos. */
export default function AdminMovedLayout({ children }: { children: ReactNode }) {
  return children;
}
